@model eLearningLMS.Models.SmallButtonModel

<a href="@Url.Action(Model.Action)@Model.ActionParemeters" type="button" class="btn @Model.ButtonType btn-sm">
    <span class="glyphicon glyphicon-@Model.Glyph"></span>
    <span class="sr-only">@Model.Text</span>
</a>

